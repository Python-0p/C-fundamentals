#include <stdio.h>

int main()
{

    int a = 10, b = 20;

    int Sum = a+b;
    int Sub = a-b;
    int Multiplication = a*b;
    int divide = a/b;

    printf("%d is Sum.\n", Sum);
    printf("%d is Sub.\n", Sub);
    printf("%d is Multiplication.\n", Multiplication);
    printf("%d is Divide.\n", divide);

    return 0;
}
