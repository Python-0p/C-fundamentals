#include <stdio.h>

int main(){
int a = 1, b = 2;

int c = a>b; // Here c has a Value which is decided by comparing of a and b.
printf("%d\n", c);
/* output will be 0 bcuz A is not greater than B
thier are Multiple Relational Operators we Can use in C language like 
==, >, <, <=, >=, !=
all of them are used to compare between Variables and give result between 0 and 1.*/
return 0;
}
